# Copy this to .env for local development.
# No secrets are required — Open-Meteo is free and keyless.
# This file is safe to commit; .env is gitignored.

# Override the port if needed locally (Cloud Run always uses 8080)
PORT=8080
